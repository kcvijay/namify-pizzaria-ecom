import { Lemon, Manrope } from 'next/font/google';

export const lemon = Lemon({ subsets: ['latin'], weight: ['400'] });
export const manrope = Manrope({
  subsets: ['latin'],
  weight: ['300', '400', '700'],
});
